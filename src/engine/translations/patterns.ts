/** คำแปลของข้อความที่มีตัวเลขแทรก เก็บโดยแทนตัวเลขทุกตัวด้วย # (ลำดับ # ต้องตรงกับต้นฉบับ) */
export const PATTERNS: Record<string, string> = {
  'วงจรนี้ประกอบด้วย # อุปกรณ์ (จากแหล่งจ่ายไปทางโหลด):': 'This circuit has # components (from the source toward the load):',
  '#. Resistor (ตัวต้านทาน) — ต่ออนุกรม': '#. Resistor — in series',
  '#. Inductor (ตัวเหนี่ยวนำ) — ต่ออนุกรม': '#. Inductor — in series',
  'Z_{\\text{เริ่มต้น}} = # + j#\\,\\Omega': 'Z_{\\text{start}} = # + j#\\,\\Omega',
  'ขั้นถัดไปจะไล่ทีละตัวรวม # ตัว ดูค่า Z ที่มองเห็นเปลี่ยนไปทีละก้าว': 'The next steps work through all # of them one at a time, so you can watch the impedance you see change step by step',
  'Z_{\\text{ใหม่}} = Z_{\\text{เดิม}} + Z_{L} = # + j# + (# + j#)': 'Z_{\\text{new}} = Z_{\\text{old}} + Z_{L} = # + j# + (# + j#)',
  'Z_{\\text{ใหม่}} = Z_{\\text{เดิม}} + Z_{R} = # + j# + (# + j#)': 'Z_{\\text{new}} = Z_{\\text{old}} + Z_{R} = # + j# + (# + j#)',
  'ส่วนจินตภาพบอกเส้นโค้งรีแอกแตนซ์คงที่ (constant-x arc): x > # อยู่ครึ่งบน (inductive), x < # อยู่ครึ่งล่าง (capacitive)': 'The imaginary part picks the constant-x arc: x > # is the upper half (inductive), x < # the lower half (capacitive)',
  'x = # → จุดอยู่บนแกนนอน (แกน resistance) พอดี': 'x = # → the point lies exactly on the horizontal axis (the resistance axis)',
  'ตำแหน่งบนสเกล wavelengths toward generator ≈ # λ': 'Position on the wavelengths-toward-generator scale ≈ # λ',
  '\\text{อ่านตรงเส้นนี้:}\\; |\\Gamma| = #,\\; SWR = #,\\; RL = #\\ \\text{dB},\\; \\text{กำลังสะท้อน}\\ |\\Gamma|^# = #\\%': '\\text{Read along this line:}\\; |\\Gamma| = #,\\; SWR = #,\\; RL = #\\ \\text{dB},\\; \\text{reflected power}\\ |\\Gamma|^# = #\\%',
  'Mismatch loss ไม่มีแถวของตัวเองบนแถบในแอปนี้ ค่า # dB ข้างต้นจึงมาจากสูตร −#·log₁₀(# − |Γ|²) ที่แอปคำนวณให้': 'Mismatch loss has no strip of its own in this app, so the # dB above comes from the formula −#·log₁₀(# − |Γ|²), which the app works out for you',
  'z_in เป็นจำนวนจริงแต่ ≠ #: ใช้หม้อแปลง λ/# ที่มี Z_t = √(Z₀·R) ได้เลย': 'z_in is real but ≠ #: a λ/# transformer with Z_t = √(Z₀·R) will do the job',
  '#. Capacitor (ตัวเก็บประจุ) — ต่ออนุกรม': '#. Capacitor — in series',
  'Z_{\\text{ใหม่}} = Z_{\\text{เดิม}} + Z_{C} = # + j# + (# - j#e+#)': 'Z_{\\text{new}} = Z_{\\text{old}} + Z_{C} = # + j# + (# - j#e+#)',
  '\\text{อ่านตรงเส้นนี้:}\\; |\\Gamma| = #,\\; SWR = \\infty,\\; RL = #\\ \\text{dB},\\; \\text{กำลังสะท้อน}\\ |\\Gamma|^# = #\\%': '\\text{Read along this line:}\\; |\\Gamma| = #,\\; SWR = \\infty,\\; RL = #\\ \\text{dB},\\; \\text{reflected power}\\ |\\Gamma|^# = #\\%',
  'Mismatch loss ไม่มีแถวของตัวเองบนแถบในแอปนี้ ค่า ∞ dB ข้างต้นจึงมาจากสูตร −#·log₁₀(# − |Γ|²) ที่แอปคำนวณให้': 'Mismatch loss has no strip of its own in this app, so the ∞ dB above comes from the formula −#·log₁₀(# − |Γ|²), which the app works out for you',
  'จุดอยู่บนวงกลม r = # แล้ว: ใส่รีแอกแตนซ์อนุกรมที่หักล้าง x ก็จะถึงศูนย์กลาง': 'The point is already on the r = # circle: add a series reactance that cancels x and you reach the centre',
  '#. Resistor (ตัวต้านทาน) — ต่อขนานลงกราวด์': '#. Resistor — in shunt to ground',
  '#. Capacitor (ตัวเก็บประจุ) — ต่อขนานลงกราวด์': '#. Capacitor — in shunt to ground',
  'อุปกรณ์ทุกตัวต่อขนานกัน จึงสะดวกกว่าถ้าคิดเป็น admittance (Y = #/Z) แล้วบวกกัน': 'Everything here is in parallel, so it is easier to work in admittance (Y = #/Z) and add',
  'Y_{\\text{เดิม}} = \\frac{#}{Z_{\\text{เดิม}}} = \\frac{#}{\\infty} = # + j#\\ \\text{S}': 'Y_{\\text{old}} = \\frac{#}{Z_{\\text{old}}} = \\frac{#}{\\infty} = # + j#\\ \\text{S}',
  'Y_{\\text{ใหม่}} = Y_{\\text{เดิม}} + Y_{C} = # + j# + (# + j#) = # + j#\\ \\text{S}': 'Y_{\\text{new}} = Y_{\\text{old}} + Y_{C} = # + j# + (# + j#) = # + j#\\ \\text{S}',
  'Z = \\frac{#}{Y_{\\text{ใหม่}}} = # - j#e+#\\,\\Omega': 'Z = \\frac{#}{Y_{\\text{new}}} = # - j#e+#\\,\\Omega',
  'Y_{\\text{เดิม}} = \\frac{#}{Z_{\\text{เดิม}}} = \\frac{#}{# - j#e+#} = # + j#\\ \\text{S}': 'Y_{\\text{old}} = \\frac{#}{Z_{\\text{old}}} = \\frac{#}{# - j#e+#} = # + j#\\ \\text{S}',
  'Y_{\\text{ใหม่}} = Y_{\\text{เดิม}} + Y_{R} = # + j# + (# + j#) = # + j#\\ \\text{S}': 'Y_{\\text{new}} = Y_{\\text{old}} + Y_{R} = # + j# + (# + j#) = # + j#\\ \\text{S}',
  'Z = \\frac{#}{Y_{\\text{ใหม่}}} = # - j#\\,\\Omega': 'Z = \\frac{#}{Y_{\\text{new}}} = # - j#\\,\\Omega',
  'B ≈ # → Parallel resonance: อิมพีแดนซ์เหลือแต่ส่วนจริง (สูงสุด)': 'B ≈ # → parallel resonance: only the real part of the impedance is left (at its maximum)',
  '#. Inductor (ตัวเหนี่ยวนำ) — ต่อขนานลงกราวด์': '#. Inductor — in shunt to ground',
  'Y_{\\text{เดิม}} = \\frac{#}{Z_{\\text{เดิม}}} = \\frac{#}{# + j#} = # + j#\\ \\text{S}': 'Y_{\\text{old}} = \\frac{#}{Z_{\\text{old}}} = \\frac{#}{# + j#} = # + j#\\ \\text{S}',
  'Y_{\\text{ใหม่}} = Y_{\\text{เดิม}} + Y_{L} = # + j# + (# - j#e+#) = # - j#e+#\\ \\text{S}': 'Y_{\\text{new}} = Y_{\\text{old}} + Y_{L} = # + j# + (# - j#e+#) = # - j#e+#\\ \\text{S}',
  'Z = \\frac{#}{Y_{\\text{ใหม่}}} = # + j#\\,\\Omega': 'Z = \\frac{#}{Y_{\\text{new}}} = # + j#\\,\\Omega',
  '#. Transmission Line (สายส่ง)': '#. Transmission Line',
  '#. Load Z_L (โหลด / สายอากาศ) — ต่ออนุกรม': '#. Load Z_L (load / antenna) — in series',
  'สายส่ง Z₀ = # Ω ยาว l = # λ (ไร้การสูญเสีย)': 'A line of Z₀ = # Ω and length l = # λ (lossless)',
  'บน Smith Chart: เดินตามวงกลม SWR คงที่ ไปทาง generator (ตามเข็มนาฬิกา) เป็นระยะ l บนสเกลรอบนอก ซึ่งเท่ากับมุม #βl': 'On the Smith chart: travel round the constant-SWR circle toward generator (clockwise) by l on the outer scale, which is an angle of #βl',
  'บนสเกล: จาก # λ เดินไปอีก # λ → # λ (toward generator)': 'On the scale: from # λ, go a further # λ → # λ (toward generator)',
  '#. λ/# Transformer (หม้อแปลงหนึ่งในสี่คลื่น)': '#. λ/# Transformer (quarter-wave transformer)',
  'หม้อแปลง λ/# (Quarter-Wave Transformer)': 'λ/# transformer (quarter-wave transformer)',
  'เลือก Z_t = √(Z₀·R_L) = √(#×#) = # Ω จะได้ Z_in = Z₀ พอดี (ถ้า Z_L เป็นจำนวนจริง)': 'Choose Z_t = √(Z₀·R_L) = √(#×#) = # Ω and you get exactly Z_in = Z₀ (provided Z_L is real)',
  'จุด z_in เกือบทับศูนย์กลาง รัศมีเกือบเป็นศูนย์ จึงไม่ต้องกางวงเวียน: อ่านทุกค่าที่ปลายซ้ายสุดของแถบด้านล่าง (SWR ≈ # กำลังสะท้อนเกือบ #)': 'z_in sits almost on the centre, with a radius of almost zero, so there is nothing to step off with dividers: read every value at the far left of the strip below (SWR ≈ #, reflected power almost #)',
  '#. Short Stub (สตับปลายลัดวงจร) — ต่อขนานลงกราวด์': '#. Short Stub — in shunt to ground',
  'สตับต่อขนาน จึงทำงานในโดเมน admittance: y = #/z (ใช้กราฟ Y หรือหมุนจุด #°)': 'The stub is in shunt, so it works in the admittance domain: y = #/z (use the Y grid, or rotate the point by #°)',
  'ความยาวสตับ l = # λ → βl = #°': 'Stub length l = # λ → βl = #°',
  'ก่อนใส่สตับ y อยู่บนวงกลม g = # แล้ว (g ≈ #) จึงต้องการสตับที่ให้ b = −# เพื่อหักล้าง': 'Before the stub, y is already on the g = # circle (g ≈ #), so the stub has to give b = −# to cancel it',
  '#. Open Stub (สตับปลายเปิด) — ต่อขนานลงกราวด์': '#. Open Stub — in shunt to ground',
  '#. Short Stub (สตับปลายลัดวงจร) — ต่ออนุกรม': '#. Short Stub — in series',
  'สตับที่ต่ออนุกรมกับสาย ทำหน้าที่เป็นรีแอกแตนซ์อนุกรม: ปลายลัด X = Z₀tan βl (βl = #°)': 'A stub in series with the line acts as a series reactance: shorted, X = Z₀tan βl (βl = #°)',
  '#. Antenna (curve) (สายอากาศแบบตารางหลายความถี่) — ต่ออนุกรม': '#. Antenna (curve) (tabulated multi-frequency antenna) — in series',
  'อิมพีแดนซ์ของสายอากาศเปลี่ยนตามความถี่ ตารางมี # จุด (#–# MHz) ค่าที่ f = # MHz ได้จากการประมาณเชิงเส้นระหว่างจุดที่ใกล้ที่สุด': 'The antenna\'s impedance varies with frequency. The table has # points (#–# MHz)\\; the value at f = # MHz is linearly interpolated between the two nearest points.',
  'จัดโครงวงจรตามเฉลย: λ/# — R': 'Arrange the circuit as in the solution: λ/# — R',
  'ตั้ง f = # MHz, Z₀ = # Ω': 'Set f = # MHz, Z₀ = # Ω',
  'จัดโครงวงจรตามเฉลย: S#↓ (ใกล้แหล่งจ่าย) — TL# (ใกล้แหล่งจ่าย) — S#↓ (ใกล้โหลด) — TL# (ใกล้โหลด) — Z_L': 'Arrange the circuit as in the solution: S#↓ (near the source) — TL# (near the source) — S#↓ (near the load) — TL# (near the load) — Z_L',
  'จัดโครงวงจรตามเฉลย: C#↓ (ใกล้แหล่งจ่าย) — L — C#↓ (ใกล้โหลด) — Z_L': 'Arrange the circuit as in the solution: C#↓ (near the source) — L — C#↓ (near the load) — Z_L',
  'จัดโครงวงจรตามเฉลย: L# (ใกล้แหล่งจ่าย) — L#↓ (ใกล้โหลด) — ANT': 'Arrange the circuit as in the solution: L# (near the source) — L#↓ (near the load) — ANT',
  'จุดวัดใหม่: z = #': 'New measurement point: z = #',
  'โหลดเดิม: y = #': 'The original load: y = #',
  'แอดมิตแตนซ์ที่จุดใหม่ y = #': 'Admittance at the new point, y = #',
  '# จุด · # # j# Ω @ # MHz': '# points · # # j# Ω @ # MHz',
  'ทำไม่ได้: #': 'Not possible: #',
  'โหลด: z = #': 'Load: z = #',
  'ตอนนี้ g รวม = # และ b รวม = # ลองเทียบกับ # และ # หากยังไม่ตรง จุดรวมจะอยู่ห่างศูนย์กลาง สตับชดเชย b ได้ แต่ไม่สามารถแก้ g ให้เป็น # ได้เอง': 'At the moment the total g = # and the total b = #. Compare them with # and #: if they do not agree, the combined point sits away from the centre. A stub can make up for b, but it cannot fix g to # on its own.',
  'เปรียบเทียบคลื่นที่ # MHz ยาว # เมตร กับ # MHz ยาว # เมตร บนแกนระยะเดียวกัน': 'Comparing a wave at # MHz, # metres long, with one at # MHz, # metres long, on the same distance axis',
  'จากเครื่องส่งผ่านจุดต่อสตับ ไปตามสายหลักระยะ # เมตรถึงโหลด แขนงสตับยาว # เมตร': 'From the transmitter, through the stub junction, along # metres of main line to the load, with a # metre stub branch',
  '· Example ## ใช้ความยาวปัดตามหนังสือ จึงได้ SWR ที่ # MHz = # (ใกล้ #)': '· Example ## uses the book\'s rounded lengths, so the SWR at # MHz comes out as # (close to #)',
  'เปิดคอร์ส: #': 'Open the course: #',
  'โจทย์ #': 'Problem #',
  'เปิดเนื้อหาในคอร์ส: #': 'Open this part of the course: #',
  'λ₀ (อากาศ) = #': 'λ₀ (in air) = #',
  'y ที่ตำแหน่งสตับ (ก่อนใส่สตับ) = #': 'y at the stub position (before the stub is added) = #',
  '#) #ที่โหลด: #↓=#, #=#': '#) #at the load: #↓=#, #=#',
  'จุดตัดที่ # · # λ': 'Crossing at # · # λ',
  'ใช้อุปกรณ์ทั้ง # ตัวเป็นโหลด': 'Treat all # components as the load',
  'ใช้อุปกรณ์ท้ายวงจร # ตัวเป็นโหลด': 'Treat the last # components as the load',
  'เปิดเนื้อหา: #': 'Open this content: #',
  'SWR = # · โหลดนี้ยังไม่แมตช์กับสาย # Ω': 'SWR = # · this load is not yet matched to the # Ω line',
  'ที่จุดนี้ y = # + j# ส่วนจริงถูกต้องแล้ว แต่ยังมีส่วนจินตภาพอยู่ จึงต้องใช้สตับหักล้างส่วนนี้ บนวง SWR หนึ่งรอบเท่ากับระยะสาย #λ จึงใช้มุมที่หมุนหารด้วย #° เพื่อหาระยะเป็น λ': 'At this point y = # + j#. The real part is right, but an imaginary part is left, so a stub has to cancel it. One lap of the SWR circle is #λ of line, so divide the angle turned by #° to get the distance in λ.',
  'ได้ตำแหน่งสตับ d = #λ · SWR ของสายฝั่งโหลดยังเป็น #': 'The stub position comes out as d = #λ · the SWR on the load side of the line is still #',
  'สายที่ตำแหน่งต่อสตับมี b = +# ดังนั้นสตับต้องให้ b = −# ขนาดเท่ากันและเครื่องหมายตรงข้าม เมื่อบวกกันจึงเหลือศูนย์': 'The line at the stub junction has b = +#, so the stub must give b = −# — the same size, the opposite sign — so that they add to zero',
  'ต้องการ y_stub = −j# · ค่า b เป็นค่าปกติ ไม่มีหน่วย': 'You need y_stub = −j# · b is a normalised value, with no units',
  'หยุดเมื่อกริด Y อ่าน b = −# ได้ความยาว #λ โดยใช้สตับ Z₀ = # Ω เท่ากับสายหลัก ที่ความยาวศูนย์ y มีขนาดอนันต์ จึงอ่านค่า b จำกัดได้หลังเริ่มเดินจาก SHORT แล้ว': 'Stop when the Y grid reads b = −#, which gives a length of #λ, using a stub of Z₀ = # Ω, the same as the main line. At zero length y is infinite, so a finite b can only be read once you have set off from SHORT.',
  'ได้สตับปลายลัด l_s ≈ #λ · ขอบกราฟนี้เป็นของสตับ ไม่ใช่วง SWR ของโหลด': 'The shorted stub comes out as l_s ≈ #λ · this rim belongs to the stub, not to the load\'s SWR circle',
  'ฝั่งเครื่องส่งก่อนถึงจุดต่อสตับมี SWR = # แต่ช่วงระหว่างสตับกับโหลดยังมี SWR = # การแมตช์ทำให้เครื่องส่งเห็นอิมพีแดนซ์ที่ถูกต้อง โดยโหลดเดิมยังเป็น # − j# Ω': 'On the transmitter side, before the stub junction, SWR = #, while between the stub and the load it is still #. Matching makes the transmitter see the right impedance, while the load itself is still # − j# Ω',
  'หมุนจุดช่วยอ่าน #° / #° · ไม่ใช่ระยะสาย': 'Rotating the point to read #° / #° · this is not a distance along the line',
  'b สาย = +# → ต้องเพิ่ม b สตับ = −#': 'Line b = +# → the stub must add b = −#',
  'l_s = #λ · b_s = #': 'l_s = #λ · b_s = #',
  'ชดเชย #% · y รวม = # · SWR = #': 'Compensated #% · total y = # · SWR = #',
  'กดเพื่ออ่านค่าของจุดนี้ — #': 'Press to read this point\'s values — #',
  '✓ ทั้งแบนด์ SWR ≤ #': '✓ SWR ≤ # across the whole band',
  '✗ แบนด์เกิน SWR #': '✗ The band exceeds SWR #',
  'กดเพื่ออ่านค่า #': 'Press to read the value #',
  '|Γ| ของ # =': '|Γ| of # =',
  '(ตอนนี้อยู่ขั้นที่ #)': '(currently at step #)',
  'ยังอยู่ในช่วง # ถึงต่ำกว่า # → # λ': 'Still between # and just under # → # λ',
  'L-section: C อนุกรม (ชิดโหลด) → C ขนาน': 'L-section: series C (nearest the load) → shunt C',
  'C ขนาน = # pF': 'shunt C = # pF',
  'C อนุกรม = # nF': 'series C = # nF',
  'โหลดเดิม: R — L': 'The original load: R — L',
  'R_L = # Ω < Z₀ = # Ω → เริ่มด้วยอุปกรณ์อนุกรมเพื่อพาจุดไปตัดวงกลม g = #': 'R_L = # Ω < Z₀ = # Ω → start with a series element to bring the point onto the g = # circle',
  'b (ขนาน) = # · x (อนุกรม) = #': 'b (shunt) = # · x (series) = #',
  'L-section: C อนุกรม (ชิดโหลด) → L ขนาน': 'L-section: series C (nearest the load) → shunt L',
  'L ขนาน = # nH': 'shunt L = # nH',
  'C อนุกรม = # pF': 'series C = # pF',
  'b (ขนาน) = −# · x (อนุกรม) = −#': 'b (shunt) = −# · x (series) = −#',
  'สตับปลายลัด: สาย # λ + สตับ # λ': 'Shorted stub: # λ of line + a # λ stub',
  'สตับปลายลัด # Ω ยาว # λ': 'Shorted stub, # Ω, # λ long',
  'สายส่ง # Ω ยาว # λ': '# Ω line, # λ long',
  'เดินจากโหลดไปทาง generator # λ จนถึงวงกลม g = # (y = # − j#)': 'Walk # λ from the load toward generator to reach the g = # circle (y = # − j#)',
  'สตับต้องให้ b = # → ความยาว # λ': 'The stub must give b = # → a length of # λ',
  'สตับปลายเปิด: สาย # λ + สตับ # λ': 'Open stub: # λ of line + a # λ stub',
  'สตับปลายเปิด # Ω ยาว # λ': 'Open stub, # Ω, # λ long',
  'อุปกรณ์ขนานตัวเดียว: C': 'A single shunt element: C',
  'ต้องการ b = # (B = # S)': 'You need b = # (B = # S)',
  'λ/# + สาย # λ: Z_t = # Ω': 'λ/# + # λ of line: Z_t = # Ω',
  'หม้อแปลง λ/#, Z_t = # Ω': 'λ/# transformer, Z_t = # Ω',
  'โหลดไม่ใช่จำนวนจริง จึงต่อสาย # λ ก่อนเพื่อหมุนจุดมาที่แกนนอน ได้ R = # Ω': 'The load is not real, so # λ of line goes in first to rotate the point onto the horizontal axis, giving R = # Ω',
  'เดินจากโหลดไปทาง generator # λ จนถึงวงกลม g = # (y = # + j#)': 'Walk # λ from the load toward generator to reach the g = # circle (y = # + j#)',
  'สตับต้องให้ b = −# → ความยาว # λ': 'The stub must give b = −# → a length of # λ',
  'L อนุกรม = # H': 'series L = # H',
  'ต้องการ b = −# (B = −# S)': 'You need b = −# (B = −# S)',
  'b (ขนาน) = −# · x (อนุกรม) = #': 'b (shunt) = −# · x (series) = #',
  'L อนุกรม = # nH': 'series L = # nH',
  'R_L = # Ω > Z₀ = # Ω → โหลดอยู่ในวงกลม r = # จึงเริ่มด้วยอุปกรณ์ขนานเพื่อพาจุดออกไปตัดวงกลม r = #': 'R_L = # Ω > Z₀ = # Ω → the load lies inside the r = # circle, so start with a shunt element to take the point out to the r = # circle',
  'หม้อแปลง λ/#: Z_t = # Ω': 'λ/# transformer: Z_t = # Ω',
  'โหลดเป็นจำนวนจริง R = # Ω อยู่แล้ว': 'The load is already real, R = # Ω',
};
